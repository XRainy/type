<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.type.mapper.user.SelfRecordDao" >
    <insert id="insert" parameterType="com.type.pojo.SelfRecord">
        INSERT INTO SelfRecord (selfRecordId,userId,userName,score,createTime)
        VALUES (
        #{selfRecordId,jdbcType=VARCHAR},
        #{userId,jdbcType=VARCHAR},
        #{userName,jdbcType=VARCHAR},
        #{score,jdbcType=INTEGER},
        #{createTime,jdbcType=TIMESTAMP})
    </insert>
    <select id="selectByUserId" resultType="com.type.pojo.SelfRecord" parameterType="string">
        SELECT * FROM SelfRecord WHERE userId=#{userId} ORDER BY createTime DESC
    </select>
</mapper>